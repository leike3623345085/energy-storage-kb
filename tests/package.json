{
  "name": "smart-sheet-analyzer-tests",
  "version": "1.0.0",
  "description": "Test suite for Smart Sheet Analyzer JavaScript utilities",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.0.0",
    "@vitest/ui": "^1.0.0",
    "vitest": "^1.0.0"
  }
}
